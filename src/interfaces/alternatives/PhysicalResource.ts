/**
 * Interface DigitalResource is used to represent a physical resource
 * in the system.
 */

interface PhysicalResource {
  identifier: string,
  found: boolean,
}

export default PhysicalResource;

/**
 * Used to determine if a toolbar button is active or not
 * and what value to display in the badge.
 */
type ToolbarActive = {
  active: boolean,
  value: string,
  isDotOnly: boolean,
};

export default ToolbarActive;

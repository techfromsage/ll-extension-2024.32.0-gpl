/**
 * Interface TocAlertSettings represents the settings from the API for an institution's TOC Alerts.
 */
interface TocAlertSettings {
  access: boolean,
  text: string,
}

export default TocAlertSettings;

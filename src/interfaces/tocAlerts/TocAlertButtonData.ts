/**
 * Interface TocAlertButtonData are the values needed to represent a Toc alert button in the system.
 */
interface TocAlertButtonData {
  position: number,
  title: string,
  journal: string,
  text: string,
  url: string,
}

export default TocAlertButtonData;

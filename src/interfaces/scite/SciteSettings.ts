/**
 * Interface SciteSettings represents the settings from the API for Scite integration.
 */
interface SciteSettings {
  elsewhereEnabled: boolean,
  enabled: boolean,
  googleScholarEnabled: boolean,
}

export default SciteSettings;

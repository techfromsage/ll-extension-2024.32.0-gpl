/**
 * Enum FloatingActionPosition standardises the placement of the floating action button
 */
enum FloatingActionPosition {
  BottomRight = 'bottom-right', // default
  BottomLeft = 'bottom-left',
  TopLeft = 'top-left',
  Off = 'off',
}

export default FloatingActionPosition;

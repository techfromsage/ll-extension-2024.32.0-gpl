/**
 * Enum ComponentType standardises the types/variations of a component.
 */
enum ComponentType {
  Success = 'success',
  Warning = 'warning',
  Info = 'info',
  Error = 'error',
  NewFeature = 'new-feature',
}

export default ComponentType;

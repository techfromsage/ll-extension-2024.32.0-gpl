/**
 * Represents the different states the settings form can be in.
 */
enum SettingsFormState {
  Ready = 'ready',
  Pending = 'pending',
  Submitted = 'submitted',
}

export default SettingsFormState;

/**
 * Listener callback - runs when the reference manager is switched on.
 */
import ReferenceManagerContext from '@/interfaces/stateMachine/ReferenceManagerContext';
import { ReferenceManagerEventSchema } from '@/modules/shared/stateMachine/StateMachineReferenceManager';

/**
 * Listener callback - runs when the reference manager is switched on.
 */
export default async (context: ReferenceManagerContext, event: ReferenceManagerEventSchema) => {
};

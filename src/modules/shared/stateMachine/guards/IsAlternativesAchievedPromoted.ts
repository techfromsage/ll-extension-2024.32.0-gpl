import FeaturesContext from '@/interfaces/stateMachine/FeaturesContext';

/**
 * To be implemented.
 *
 * Checks if the isAlternativesAchievedPromoted state is valid, based on the settings of the institution from the context.
 *
 *
 *
 * @param context
 * @constructor
 */
export default (context: FeaturesContext): boolean => {
  return false;
};
